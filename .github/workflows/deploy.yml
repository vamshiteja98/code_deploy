name: Node.js Deploy to EC2 via CodeDeploy

on:
  push:
    branches:
      - main # or your default branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v3

      - name: 🧱 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Install dependencies
        run: npm install

      - name: 🛠 Make scripts executable
        run: chmod +x scripts/*.sh

      - name: 📁 Create deployment package
        run: |
          mkdir -p deploy
          cp -r * deploy/
          cp -r scripts deploy/
          cp appspec.yml deploy/
          cd deploy
          zip -r node-app.zip .

      - name: ☁️ Upload to S3
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --acl private --follow-symlinks
        env:
          AWS_S3_BUCKET: my-apache-app
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ap-south-1
          SOURCE_DIR: deploy
          DEST_DIR: /

      - name: 🚀 Deploy to CodeDeploy using AWS CLI
        run: |
          aws deploy create-deployment \
            --application-name code-deploy-apache-application \
            --deployment-group-name code-deploy-apache-group \
            --s3-location bucket=my-apache-app,key=node-app.zip,bundleType=zip \
            --region ap-south-1
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
